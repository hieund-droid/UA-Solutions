{
  "_comment": "HTTP-service-only deps. MERGE these into the project's package.json (dependencies + devDependencies) when you convert the project to an HTTP service. After merging, run `pnpm install`.",
  "dependencies": {
    "express": "^4.21.1",
    "express-async-errors": "^3.1.1",
    "helmet": "^8.0.0",
    "pino-http": "^10.3.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/supertest": "^6.0.2",
    "supertest": "^7.0.0"
  }
}
