#!/usr/bin/env bash
# UserPromptSubmit hook — fires on every user message. Ultra-terse.
cat <<'EOF'
<apero>Apero: rules → ./CLAUDE.md + ./CLAUDE.AndrejKarpathy.md (never edit) · default stack: Python · entry obvious (python main.py) · deps in manifest only — AI never installs · logger required (no print/console.log) · #devops for ops</apero>
EOF
