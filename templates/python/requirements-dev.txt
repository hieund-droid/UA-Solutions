# Dev-only dependencies (lint, type-check, test).
# Install with:
#   python -m pip install -r requirements-dev.txt
# (requirements.txt is included so a single command sets up everything)

-r requirements.txt

pytest==8.3.4
pytest-asyncio==0.24.0
ruff==0.7.4
mypy==1.13.0
