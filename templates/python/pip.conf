# Apero internal Python package mirror (artifact-keeper, security-scanned).
# pip reads this file when it sits at the project root.

[global]
index-url = https://artifact-keeper.aperogroup.ai/api/v1/repositories/pypi-proxy/download/simple/
trusted-host = artifact-keeper.aperogroup.ai
