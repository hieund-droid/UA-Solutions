<!--
This file is the header used by each per-stack template's CLAUDE.md. The
bootstrap CLI prepends the master CLAUDE.md (from the repo root) to the
stack-specific addendum so the generated project gets a single, self-contained
CLAUDE.md file.

If you copy a template by hand, copy this repo's root CLAUDE.md into the new
project's CLAUDE.md and then append the addendum from templates/<stack>/CLAUDE.md.
-->
